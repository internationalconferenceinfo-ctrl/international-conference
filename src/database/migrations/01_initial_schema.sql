-- Migration 01: Initial Schema Setup
-- Applies initial tables for conferences, organizers, categories, subscribers, and user feedback.

SELECT '01_initial_schema executed successfully' AS migration_status;
